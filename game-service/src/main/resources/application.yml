spring:
  profiles:
    active: dev
  application:
    name: GAME-SERVICE
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    properties:
      dialect: org.hibernate.dialect.MySQLDialect

